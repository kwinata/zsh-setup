declare -a scripts=(
  "git/user_config"
  "git/shortcuts"
  "zsh/bullet_train"
  "cpp/shortcuts"
  "shortcuts/commands"
  "shortcuts/shopee"
)

for file in "${scripts[@]}"
do
  source $HOME/zsh-setup/$file
done

HOMEBREW_NO_AUTO_UPDATE=1
