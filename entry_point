source $HOME/zsh-setup/extra_config

declare -a scripts=(
  "shortcuts/apps"
  "shortcuts/cpp"
  "shortcuts/git"
  "shortcuts/go"
  "shortcuts/shopee"

  "envs/bullet_train"
)

for file in "${scripts[@]}"
do
  source $HOME/zsh-setup/$file
done
